import { UnifiedShellLayout } from '@/app/components/UnifiedShellLayout'

export default function MusicGroupLayout({ children }: { children: React.ReactNode }) {
  return <UnifiedShellLayout>{children}</UnifiedShellLayout>
}
