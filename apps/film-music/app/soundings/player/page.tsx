import { ExtensionPlaceholder } from '@/app/components/ExtensionPlaceholder'

export default function MusicPlayerPage() {
  return (
    <ExtensionPlaceholder
      title="Player"
      mode="music"
      note="Replace with PlayerClient, queue, and suggestions from the Soundings app, fed by a music PlatformAdapter (Spotify/YouTube)."
    />
  )
}
