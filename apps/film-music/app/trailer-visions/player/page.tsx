import { ExtensionPlaceholder } from '@/app/components/ExtensionPlaceholder'

export default function MoviesPlayerPage() {
  return (
    <ExtensionPlaceholder
      title="Player"
      mode="movies"
      note="Replace with the YouTube/LLM flow from apps/trailer-vision/app/page.tsx via a movie PlatformAdapter."
    />
  )
}
