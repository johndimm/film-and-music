import { ExtensionPlaceholder } from '@/app/components/ExtensionPlaceholder'

export default function UnifiedSettingsPage() {
  return <ExtensionPlaceholder title="Settings" mode="music" note="Merge Soundings and Trailer Vision settings, or tab by kind." />
}
